doctrine:
    default_connection: local
    connections:
        local:
            auth:
                driver:   pdo_mysql
                host:     'localhost'
                port:     '3306'
                dbname:   'azerothcore_local_auth'
                user:     'root'
                password: 'root'
                charset:  UTF8
        remote:
            auth:
                driver:   pdo_mysql
                host:     'localhost'
                port:     '3306'
                dbname:   'azerothcore_remote_auth'
                user:     'root'
                password: 'root'
                charset:  UTF8
soap:
    connections:
        local:
            host: 127.0.0.1,
            port: yourport,
            user: youruser,
            pass:  yourpass,
            protocol: http
        remote:
            host: yourhost,
            port: yourport,
            user: youruser,
            pass:  yourpass,
            protocol: http